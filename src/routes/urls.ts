export enum Urls {
  HOME = "/",
  LOGIN = "/login",
  SIGNUP = "/signup",
  SIGNOUT = "/signout",
  AUTH = "/auth",
  JOBS = "/jobs",
  SINGLE_JOB = "/job/:jobUlid",
  COURSES = "/courses",
  SINGLE_COURSE = "/courses/:courseUlid",
}
