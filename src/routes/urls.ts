export enum Urls {
  HOME = "/",
  LOGIN = "/login",
  SIGNUP = "/signup",
  SIGNOUT = "/signout",
  AUTH = "/auth",
  JOBS = "/jobs",
  SINGLE_JOB = "/jobs/:jobUlid",
  COURSES = "/courses",
  SINGLE_COURSE = "/courses/:courseUlid",
}
